{
  "name": "aluguel-imoveis-app",
  "version": "1.0.0",
  "author": "Carol Sulzbacher <carol.paseto@gmail.com>",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
